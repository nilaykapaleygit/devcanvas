spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=DEVCANVAS_DB;encrypt=true;trustServerCertificate=true
    #username: ${DB_USERNAME}
    #password: ${DB_PASSWORD}
    username: devcanvas_mainuser
    password: Devcanvas@123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    show-sql: true

    properties:
      hibernate:
        format_sql: true
      